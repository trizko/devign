import React from 'react';

var AppStyle = {
    display: 'flex',
    height: '100vh',
}

export default class App extends React.Component {
    render() {
        return (
            <div style={AppStyle}>
            </div>
        );
    }
}
